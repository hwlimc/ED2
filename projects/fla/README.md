# FLA Scratch

This directory is for FLA/FLAK runtime work and generated outputs. It should not
be committed to Git.

Expected local layout:

```text
scratch/fla/
  runs/
  outputs/
```

The FLAK namelists in `ED/run` write analysis and history files under
`scratch/fla/outputs`.
